{
  "name": "pdfexplorer-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "PdfExplorer task-pane client (Office.js + PDF.js). See docs/03-client.md.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint . && tsc --noEmit && tsc --noEmit -p tsconfig.test.json && tsc --noEmit -p tsconfig.node.json"
  },
  "dependencies": {
    "pdfjs-dist": "^5.4.149"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.3.0",
    "@types/office-js": "^1.0.520",
    "eslint": "^10.9.1",
    "jsdom": "^27.0.0",
    "office-addin-dev-certs": "^2.0.4",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.68.0",
    "vite": "^7.1.0",
    "vitest": "^3.2.4"
  }
}
